name: catholic_liturgy
description: A package to calculate the Catholic Liturgy for every date and obtain liturgy description in multiple languages.
version: 0.0.6
homepage: https://github.com/flavio20002/Catholic-Liturgy-Dart

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0
  enum_to_string: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  csv: ^5.0.2

flutter:
